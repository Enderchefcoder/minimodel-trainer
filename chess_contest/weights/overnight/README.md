# Overnight GM training artifacts

Generated by `python chess_contest/scripts/overnight_train.py`.

```bash
# 8h run (Stockfish sparring + self-play + winner distillation)
venv/bin/python chess_contest/scripts/overnight_train.py --hours 8

# Smoke (~10 min)
venv/bin/python chess_contest/scripts/overnight_train.py --quick
```

Outputs: `latest.json`, `gm_weights.json`, `elo_probe.json`, `overnight.log`.
