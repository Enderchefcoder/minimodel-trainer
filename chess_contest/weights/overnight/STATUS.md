# Overnight status

**Mode:** hybrid tactical-floor + pheromone residual (v3 weights), SEE/PVS search.

**Restart after insane-good upgrade** — deeper Stigmergy search (280ms / d7),
SF schedule biased to winnable 1320–2200 bands + periodic MAX distillation.

Resume from `ckpt_book.json` or `latest.json` when present.
