# Generated by scripts/generate_templates.py - edit that file, not this one.
# Dense GQA transformer at ~1.406M params — the Glint-2 crusher. Matched to the 1.4M budget with QK-norm, tied embeddings and value residuals; preferred over looped at fixed training tokens.
name: dense_1_4m
family: dense-transformer
description: "Dense GQA transformer at ~1.406M params \u2014 the Glint-2 crusher.\
  \ Matched to the 1.4M budget with QK-norm, tied embeddings and value residuals;\
  \ preferred over looped at fixed training tokens."
params: 1406506
recommended_tokens: 0.4-1B
arch:
  vocab_size: 4096
  dim: 128
  n_layers: 5
  n_heads: 8
  head_dim: 16
  n_kv_heads: 2
  ffn_hidden: 352
  window: 512
  max_seq_len: 1024
  qk_norm: true
  tie_embeddings: true
  value_residual: true
training_defaults:
  lr: 0.003
  batch_tokens: 65536
  seq_len: 1024
notes:
- 'Sandbox bake-off winner near 1.4M (tied with MHA/deep variants; GQA chosen for
  T4 throughput). Crush-glint2 v2 mix: FineWeb-Edu 55% + DCLM-100BT 28% + TinyStories
  12% + soft-label QA 5%.'
