# Generated by scripts/generate_vision_templates.py - edit that file, not this one.
# Text-conditioned diffusion transformer for 64x64 images, ~38M params.
name: dit_text_64
family: dit
description: Text-conditioned diffusion transformer for 64x64 images, ~38M params.
params: 37637424
recommended_data: pokemon-blip, diffusiondb-pixelart
arch:
  image_size: 64
  patch_size: 4
  dim: 384
  depth: 12
  n_heads: 6
  condition: text
  text_vocab_size: 4096
  text_max_len: 32
training_defaults:
  lr: 0.0002
  batch_size: 64
  max_steps: 100000
notes:
- Uses the same BPE tokenizer as the language models.
