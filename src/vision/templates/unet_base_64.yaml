# Generated by scripts/generate_vision_templates.py - edit that file, not this one.
# Convolutional UNet for 64x64 images, ~35M params.
name: unet_base_64
family: unet
description: Convolutional UNet for 64x64 images, ~35M params.
params: 36030003
recommended_data: CelebA-HQ 64, anime-faces
arch:
  image_size: 64
  base_channels: 80
  channel_multipliers:
  - 1
  - 2
  - 3
  - 4
  blocks_per_level: 2
  attention_resolutions:
  - 16
  - 8
  condition: none
training_defaults:
  lr: 0.00015
  batch_size: 32
  max_steps: 120000
