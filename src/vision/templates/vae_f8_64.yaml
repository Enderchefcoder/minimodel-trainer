# Generated by scripts/generate_vision_templates.py - edit that file, not this one.
# 8x-downsampling autoencoder for latent diffusion, ~16M params.
name: vae_f8_64
family: vae
description: 8x-downsampling autoencoder for latent diffusion, ~16M params.
params: 15826379
recommended_data: Any image corpus; train this before the latent diffusion model.
arch:
  image_size: 64
  latent_channels: 4
  base_channels: 64
  channel_multipliers:
  - 1
  - 2
  - 4
  - 4
  blocks_per_level: 2
training_defaults:
  lr: 0.0001
  batch_size: 32
  max_steps: 50000
notes:
- Train to convergence first, then freeze it and train diffusion in its latents.
